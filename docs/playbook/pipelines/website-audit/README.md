# Website Audit — moved

The Website Audit pipeline/tool now lives in its own repository:

➡️ **https://github.com/Performant-Labs/website-audit**

It was extracted from this monorepo (path `pipelines/website-audit/`) as a clean
snapshot. The full pre-extraction history remains in this repo's git log at this
path (e.g. `git log -- pipelines/website-audit`).

Issues, PRs, and further development happen in the new repo.
