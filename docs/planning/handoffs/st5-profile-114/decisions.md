# Decision journal — #114 ST-5

## O — Phase 1 (open + brief)
- **Decided:** Deliver via Views block (`user_activity`) placed on `/user/*`. Author scoping via `node_field_data.uid` contextual argument (default from URL user). Access scoping via built-in `node_access` filter (relies on gnode grants). Row = `stream_card` view mode.
- **Assumed:** `node_access` filter enforces the "outsider viewer can't see private-group content" AC without new PHP (relying on gnode's grant system). To be verified by Kernel test in T(RED).
- **Assumed:** Every seeded persona authors ≥1 node — verified in survey (Maria: 1 topic; James/Elena: events; Ravi/Sophie: docs). No seed edit expected.
- **Hedged:** If block context ("User from URL") doesn't populate the uid arg cleanly, fall back to a Views page display (local task tab at `user/%user/activity`). D can call this if wireframe forces it.
- **Evidence:** issue #114 text; `views.view.group_content_stream.yml`; do_streams README; step_700_demo_data.php lines 140, 154, 180, 286.

## D — Phase 2 (wireframe)
- **Decided:** Single low-fi ASCII wireframe (`wireframe.md`) covering four states: many (Maria demo, 3 rows), one (single row), empty, and a note that no distinct error state applies (Views-block render omission is the existing platform behavior, not new UI). Heading "Recent posts" is `<h2>`, sibling to (not nested inside) the existing contribution-stats block's own heading.
- **Decided:** Empty-state copy = **"No posts yet."** — deliberately silent on *why* (never-authored vs. access-scoped-away vs. truly none), because distinguishing those to an outsider viewer would leak the existence of inaccessible group content. This is the one truthful string that covers all three causes without a product decision needed.
- **Assumed:** No pager UI is shown for the POC (items_per_page=10 is an invisible cap, not a visible "load more" control) — matches brief's "pagination beyond a small recent cap" being out of scope.
- **Assumed:** Row secondary text ("Type · relative date") reuses the same muted-text color token as `group_content_stream`'s date column rather than introducing a new shade — flagged in WCAG notes for T/U contrast verification once CSS lands, since D cannot render final computed contrast from ASCII.
- **Evidence:** brief.md; survey.md; do_streams README (Shell contract, stream_card view mode); `views.view.group_content_stream.yml` (created/type field pattern, status filter, DESC sort convention this new view will mirror).

## O — D-gate (auto-approve per POC lean pipeline)
- **Decided:** Approve D's wireframe as-is. Heading = h2 "Recent posts", stream_card rows, empty-state copy "No posts yet." (silent on cause — access-aware).
- **Note to T/A:** D flagged an open contrast risk on the secondary "Type · date" line — reuse an existing muted-text token from `group_content_stream` styling; do NOT introduce a new shade. T should assert AA contrast in axe or manual check.
- **Evidence:** wireframe.md; POC lean pipeline (feedback-poc-lean-pipeline).

## A — Phase 3 (up-front plan review)
- **Decided:** PASS. Plan mirrors `group_content_stream` shape (base_table `node_field_data`, status=1, created DESC, block display) with the correct substitutions for a user-scoped surface: `node_field_data.uid` contextual arg (`default_argument_type: user`, `path: user/%`) + core `node_access` filter. This is not a parallel path — the analogous `group_content_stream` view is per-group by contract (gid arg via `group_relationship` relationship, path `group/%group/stream`) and cannot be parameterized to serve a user URL; `following_feed` already establishes the precedent of one dedicated view per stream surface inside do_streams.
- **Decided:** Block placement via `visibility.request_path: '/user/*'` matches the exact neighboring convention (`do_contribution_stats`, `do_profile_completeness`, `do_group_mission` — all identical shape). Correct layer.
- **Assumed:** `distinct: true` on the view query will be preserved (mirrors `group_content_stream`) to prevent row fan-out when `node_access` joins are present. T should confirm; not called out explicitly in brief.
- **Hedged:** Two low-severity notes for T (non-blocking): (a) add Kernel assertion that ordering is newest-first (created DESC) since that's a stated AC; (b) add Kernel assertion that other users' nodes don't leak in (author arg correctness). Both are cheap; not required to unblock T-RED.
- **Hedged:** HelpText target file resolution ("`do_streams/src/HelpText.php` if it exists, else `do_chrome/src/HelpText.php`") is left for F to resolve at edit time. Acceptable — trivial `ls` check.
- **Evidence:** `docs/groups/config/views.view.group_content_stream.yml` (analogous shape); `docs/groups/config/views.view.following_feed.yml` (block+stream_card row precedent, sibling view pattern); `docs/groups/config/block.block.do_contribution_stats.yml` (identical `/user/*` request_path visibility); `docs/groups/modules/do_streams/README.md` (engine contract — new view respects it, no new plugins invented).
- **Verdict:** PASS → proceed to T-RED.
## T — Phase 4 (RED)
- **Decided:** Authored two test files: `docs/groups/modules/do_streams/tests/src/Kernel/UserActivityViewTest.php` (6 tests) and `tests/e2e/profile-activity.spec.ts` (2 tests). Mapping to acceptance criteria:
  - (a) published-only -> `testPublishedOnlyExcludesUnpublishedNode`
  - (b) author scoping -> `testAuthorScopingReturnsOnlyProfileOwnersNodes`
  - (c) access-scoping (private group) -> `testAccessScopingExcludesPrivateGroupNodeForNonMember` + sanity companion `testAccessScopingIncludesPrivateGroupNodeForMember`
  - (d) newest-first ordering -> `testResultsOrderNewestFirst` (A-gate hedge)
  - (e) distinct/no fan-out -> `testDuplicateGroupRelationshipsYieldOneRowPerNode` (A-gate hedge)
  - e2e: "Recent posts" `<h2>` + Maria's 3 seeded titles + newest-first DOM order -> test 1; anonymous-viewer access-safe partial render (public-group title visible, two private-group titles absent) -> test 2.
- **Decided:** Kernel test mirrors `FollowingFeedTest.php`'s `shippedConfigDir()` walk-up pattern exactly (checks both `docs/groups/config` and `config/sync`, per this project's fixture-path convention) rather than a source-relative path, and executes the view's `block_1` display directly with an explicit `[$uid]` contextual-argument array (avoiding a full request/route stack in a Kernel test, consistent with how `StreamsScopeTest`/`FollowingFeedTest` execute display ids directly).
- **Decided:** e2e spec resolves Maria's uid at runtime from the login helper's post-login `/user/{uid}` redirect (no fixed uid in seed data, no drush shell-out from within the spec — matches how other specs in this repo capture dynamic ids from `page.url()`, e.g. `manage-members.spec.ts`, `phase3.spec.ts`).
- **Evidence gathered pre-authoring:** `docs/groups/scripts/step_700_demo_data.php` confirms "DrupalCon Portland 2026" (home of "Sprint Planning: Portland 2026") is left at DEFAULT/open visibility by Step 790, while "Core Committers" (home of "Weekly Standup Notes") is set `invite_only` and "Leadership Council" (home of "Budget Allocation Q3 2026") is set `moderated` — this is what makes the anonymous-viewer e2e test's expected partial render (1 title visible, 2 absent) correct rather than arbitrary.
- **RED verification environment:** this worktree's `.ddev/config.yaml` (`name: gm124-directory`) collided with the already-running `groups-directory-toggle-124` DDEV project (pre-existing, unrelated staleness — many older worktrees share the same committed name from a pre-per-branch-naming commit). Verified by locally renaming the project to `gm114-profile` (uncommitted, `git checkout -- .ddev/config.yaml` afterward — not part of this diff) purely to get an isolated container for RED verification; this observation is left for O/F as a housekeeping note, not fixed here.
- **RED confirmation — Kernel:** `ddev exec 'SIMPLETEST_DB=mysql://db:db@db/db php vendor/bin/phpunit -c web/core/phpunit.xml.dist --testdox web/modules/custom/do_streams/tests/src/Kernel/UserActivityViewTest.php'` (after `bash scripts/ci/assemble-config.sh` via `ddev exec`, and `ddev composer install`). All 6 tests fail identically at `shippedConfigDir()`'s `$this->fail("Could not locate shipped views.view.user_activity.yml in docs/groups/config or config/sync above ...")` — the right reason: the view genuinely does not exist anywhere in the repo yet (confirmed via `grep -rn user_activity docs/groups/config docs/groups/modules/do_streams` returning zero hits outside the new test file itself).
- **RED confirmation — e2e:** `npx playwright test --list tests/e2e/profile-activity.spec.ts` confirms both tests parse and are collected (valid TS/Playwright syntax). For a live-DOM RED, ran a **minimal** `drush site:install` (no config:import, no demo-data seed — same proportionality call T made in `docs/handoffs/111-stream-following/handoff-T-red.md` for the same class of "artifact provably absent from the repo" RED) via `BASE_URL="https://gm114-profile.ddev.site" npx playwright test tests/e2e/profile-activity.spec.ts`. Both tests fail inside `loginAndGetUid()` because `maria_chen` doesn't exist in this unseeded DB — a different trigger than the missing "Recent posts" heading, but a valid RED for the same underlying reason (the view/block/CSS provably do not exist in the tree; a full assemble→config:import→seed run was judged disproportionate for RED verification here). T(GREEN) will re-run against the fully assembled+seeded site once F has created the view, block, and CSS, which is the correct point to validate the DOM-level assertions.
- **Ready for F:** RED is valid on both fronts (Kernel: real assertion-blocking failure at the correct integration point; e2e: valid spec structure + a real, if not maximally seeded, browser-level RED for the same missing-artifact reason). F may implement against these tests.

## F — Phase 5 (implementation)

- **Decided:** `views.view.user_activity.yml` mirrors `activity_stream.yml`'s `block_1` display shape (`display_plugin: block`, its own `pager` scope, `block_description`) combined with `group_content_stream.yml`'s status/created-DESC/distinct pattern. Row plugin `entity:node` / `view_mode: stream_card` copied verbatim from `following_feed.yml` (the established block+stream_card precedent A's plan cited).
- **Decided:** Read Drupal core source directly rather than guessing the argument/validator config shape (per "read before referencing"):
  - `node_field_data.uid` argument uses plugin_id **`numeric`** (not `user_uid` — that plugin is registered on `users_field_data.uid` for a *user*-base-table view, confirmed via `web/core/modules/user/src/Plugin/views/argument/Uid.php`; wrong table for this node-base-table view).
  - `default_argument_type: user` confirmed correct via `web/core/modules/user/src/Plugin/views/argument_default/User.php` (`getParameter('user')` reads the route's `user` param — exactly what `/user/{uid}`'s `entity.user.canonical` route provides) and its schema `views.argument_default.user` (`user.views.schema.yml`).
  - **Correction to the issue's own task text:** the task said `default_argument_options.argument: 'user'`, but `user.views.schema.yml`'s `views.argument_default.user` schema defines the sub-key as **`user`** (a boolean, "Also look for a node and use the node author"), not `argument`. Followed the real schema (`default_argument_options: { user: false }`), not the literal task wording — this is what a strict-config-schema Kernel test run (see below) proves is correct.
  - `validate.type: 'entity:user'` + `validate_options: { bundles: {}, access: false, operation: view, multiple: false }` confirmed via the deriver-based `Entity` argument validator (`web/core/modules/views/src/Plugin/views/argument_validator/Entity.php`, `defineOptions()`) and its schema (`views.argument_validator.entity:*` in `views.argument_validator.schema.yml`).
  - `node_access` filter (`table: node_access, field: nid, plugin_id: node_access`) copied from Drupal core's own test fixture `web/core/modules/node/tests/modules/node_test_views/test_views/views.view.test_filter_node_access.yml` — the only real, schema-correct example of this filter anywhere in the tree.
- **Decided:** Block YAML (`block.block.do_streams_user_activity.yml`) declares **`theme: groups_chrome`**, not `bluecheese` (the theme the three existing `/user/*` blocks — `do_contribution_stats`, `do_profile_completeness`, `do_group_mission` — all use). Evidence: `config/sync/system.theme.yml` sets `default: groups_chrome` (the real active theme), and `scripts/ci/assemble-config.sh`'s own `EXCLUDE` list explicitly drops those three `bluecheese` blocks from a clean-room `config/sync` build with the comment "need the `bluecheese` theme" — meaning a `bluecheese`-themed block here would silently not ship in CI and the E2E test could never see it live. `groups_chrome` is git-tracked directly at `web/themes/custom/groups_chrome` (not gitignored, not assembled by the script) and is the theme this story's own E2E assertions run against.
- **Decided:** `label_display: visible` (schema-valid per `core.data_types.schema.yml`'s `Choice: ['0', visible]` constraint) rather than the neighboring `/user/*` blocks' `'0'`, because the wireframe explicitly requires a visible `<h2>` "Recent posts" heading (those other blocks render their own internal heading via plugin markup and hide the generic block title).
- **Decided:** Wrapper class + library attach via **`hook_preprocess_block`** (new `DoStreamsHooks::preprocessBlock()`, guarded on a new `USER_ACTIVITY_BLOCK_PLUGIN_ID` constant), not `preprocess_views_view`. Evidence: read `web/core/modules/block/templates/block.html.twig` directly — the block's own `<h2>{{ label }}</h2>` and `{{ content }}` (the view's rendered output) are SIBLINGS inside one outer `<div{{ attributes }}>`; attaching the class at the *views* level would put it on a sibling of the heading, not a wrapper around both — attaching at the *block* level gives the wireframe's single coherent "Recent posts" section (heading + rows together under one selector). Mirrors the existing `preprocessViewsView()` method's convention exactly (guard on a specific, meaningful identifier, return immediately otherwise) — no new attachment mechanism invented.
- **Decided:** `profile-activity.css` reuses the theme's existing `--gc-color-text-muted` custom property (read directly from `web/themes/custom/groups_chrome/css/stream.css` — `.gc-stream-card__byline` / `__submeta` / `__time` all already use it) for the secondary "Type · date" text, per D's flagged reuse requirement — no new rule was needed for that text itself (it's inherited from the shared `stream_card` row rendering), so the CSS file only carries container-rhythm rules (`.view-content` flex column + gap, `.stream-card-wrapper` margin reset), mirroring `following.css`'s established "small scoped tweaks only" pattern for do_streams' own CSS files.
- **Decided:** HelpText entry `profile_activity.section` appended to `do_chrome/src/HelpText.php` (append-only; zero existing keys edited). Explicitly did NOT reuse or edit the pre-registered `page.profile_stream` key (#126 W2 stub) — that key is reserved for a *different, future* dedicated profile-stream PAGE route (`view.profile_stream.page_1`) per `PageHelp::getRouteMap()`'s own docblock; this story ships a BLOCK on the existing `/user/{uid}` canonical route, a different surface entirely. Verified via `HelpTextTest.php`/`HelpTextPageKeysTest.php` that no test asserts an exhaustive/exact key set (all targeted, additive) — my new key introduces zero risk to either suite (both re-ran green, 16/16).
- **Assumed:** No dedicated `HelpText` unit test was required for the new key (T-RED's "Tests authored" table lists no such test for this story) — copy-only append, consistent with several other pre-registered/inert keys already in the file (`page.my_feed` et al.) that also ship without a bespoke per-key test beyond the general suite.
- **Found (test-authorship bug, NOT fixed — flagged for T):** `UserActivityViewTest::executeUserActivity()` calls `$view->execute([$uid])`. `ViewExecutable::execute($display_id = NULL)`'s single parameter is a **display id**, never a contextual-arguments array — passing `[$uid]` reaches `build([$uid])` → `setDisplay([$uid])` → `chooseDisplay([$uid])`, which (since the argument now IS an array) loops `foreach ([$uid] as $display_id)` and calls `$this->displayHandlers->get($uid)->access(...)`, fatally erroring with "Call to a member function access() on null" because no display is named e.g. `42`. Confirmed via direct core read (`ViewExecutable.php` lines 800-814, 1256-1265, 1450-1455) and by checking the ONLY established precedent in this exact module (`FollowingFeedTest.php`, `StreamsScopeTest.php`) — both call `->execute()` with **zero** arguments, never an array. The correct, minimal fix (Drupal's real, documented API — `ViewExecutable::setArguments()`/`preExecute($args = [])`, which internally calls `setArguments()`) is: `$view->preExecute([$uid]); $view->execute();` (replacing `$view->preExecute(); $view->execute([$uid]);`).
  - **Verified my production YAML is correct, not the cause:** made this exact one-line edit to a **throwaway scratch copy** of the ASSEMBLED (gitignored, regenerated) test file only — never the real source under `docs/groups/` — ran the suite, and **all 6 tests passed** (published-only, author-scoping, access-scoping include/exclude, newest-first ordering, distinct/no-fan-out — every acceptance criterion this Kernel file pins). Then re-ran `scripts/ci/assemble-config.sh` to overwrite the scratch copy back from the real, untouched source, and confirmed via `diff` that the assembled file exactly matches the committed source again. `git status --short` on the real test file shows no working-tree modification throughout (still `A`, staged by T, untouched by F) — zero test edits were made or left in place, per the "F writes no tests" rule.
- **Hedged:** Did not add a `#[RunTestsInSeparateProcesses]` attribute to `UserActivityViewTest.php` to silence its (and every sibling Kernel test's) `KernelTestBase` deprecation notice — that's a project-wide, pre-existing convention gap across all 6 `do_streams` Kernel test files (confirmed: `FollowingFeedTest`, `StreamsInstallTest`, `StreamsRankingTest`, `StreamsScopeTest`, `StreamsShellTest` all trigger the identical deprecation), not something specific to this story, and it's a test-file change outside F's remit regardless.
- **Evidence:** `web/core/modules/views/src/ViewExecutable.php`; `web/core/modules/views/src/DisplayPluginCollection.php`; `web/core/modules/views/src/Plugin/views/argument/Uid.php`; `web/core/modules/user/src/Plugin/views/argument_default/User.php`; `web/core/modules/user/src/Plugin/views/argument_validator/User.php`; `web/core/modules/views/src/Plugin/views/argument_validator/Entity.php`; `web/core/modules/user/config/schema/user.views.schema.yml`; `web/core/modules/views/config/schema/views.argument_validator.schema.yml`; `web/core/modules/views/config/schema/views.schema.yml` (views_block / items_per_page nullable-integer schema); `web/core/config/schema/core.data_types.schema.yml` (label_display Choice constraint); `web/core/modules/node/tests/modules/node_test_views/test_views/views.view.test_filter_node_access.yml`; `web/core/profiles/demo_umami/config/install/block.block.umami_views_block__promoted_items_block_1.yml`; `config/sync/system.theme.yml`; `scripts/ci/assemble-config.sh` (EXCLUDE list + bluecheese comment); `web/themes/custom/groups_chrome/css/stream.css`; `web/core/modules/block/templates/block.html.twig`; `docs/groups/modules/do_chrome/tests/src/Unit/HelpTextTest.php` / `HelpTextPageKeysTest.php`.

### Files touched (production only — no tests)

- `docs/groups/config/views.view.user_activity.yml` (new)
- `docs/groups/config/block.block.do_streams_user_activity.yml` (new)
- `docs/groups/modules/do_streams/css/profile-activity.css` (new)
- `docs/groups/modules/do_streams/do_streams.libraries.yml` (extended — added `profile_activity` library)
- `docs/groups/modules/do_streams/src/Hook/DoStreamsHooks.php` (extended — new `USER_ACTIVITY_BLOCK_PLUGIN_ID` constant + new `preprocessBlock()` method)
- `docs/groups/modules/do_chrome/src/HelpText.php` (extended — appended `profile_activity.section` key)

### Verification output (summary; full transcripts in handoff-F.md)

- Assemble: `bash scripts/ci/assemble-config.sh` (via `ddev exec`) — 130 config files copied, 14 modules copied, core.extension patched. Clean.
- Kernel (`UserActivityViewTest.php`, as authored): 6/6 still fail — confirmed the SAME test-authorship bug identified above (`execute([$uid])` misuse), NOT a regression from my code. With the one-line diagnostic-only fix applied to a scratch/throwaway copy: 6/6 PASS.
- Kernel (full `do_streams` Kernel directory, all 6 files): `Tests: 31, Errors: 6` — the 6 errors are exactly `UserActivityViewTest`'s known bug; all 25 other tests across `FollowingFeedTest`, `StreamsInstallTest`, `StreamsRankingTest`, `StreamsScopeTest`, `StreamsShellTest` pass. Zero regressions.
- Unit (`do_chrome` `HelpTextTest.php` + `HelpTextPageKeysTest.php`): 16/16 pass. Zero regressions from the appended key.
- Lint (`phpcs --standard=Drupal,DrupalPractice`) on both touched PHP files: `DoStreamsHooks.php` — 0 errors, 4 warnings (all 4 are pre-existing `\Drupal::` static-call advisories on code I did not touch, lines 134/163/195/312). `HelpText.php` — 0 errors/warnings in my appended line range (289-308); all reported issues are pre-existing, outside my change.
- `php -l` syntax check on both files: clean.
- Confirmed `$strictConfigSchema = TRUE` (Kernel test default, never overridden in this suite) was active throughout every run — since the view's `->save()` in `setUp()` never threw a schema-validation exception across all 6 tests, this is strong evidence `views.view.user_activity.yml` and `block.block.do_streams_user_activity.yml` are both strictly config-schema-valid.
## T — Phase 6 (GREEN + Tier 2)

- **Decided:** Repaired the test-authorship bug F flagged (not F's job to fix). `UserActivityViewTest::executeUserActivity()`'s single call site changed from `$view->preExecute(); $view->execute([$uid]);` to `$view->preExecute([$uid]); $view->execute();` — `ViewExecutable::execute($display_id = NULL)`'s only parameter is a display id, confirmed independently by reading `ViewExecutable.php` myself (agrees with F's diagnosis exactly). Purely mechanical: 2 lines changed + 3 comment lines added, zero assertions touched/weakened.
- **Kernel GREEN:** `ddev exec 'SIMPLETEST_DB=mysql://db:db@db/db php vendor/bin/phpunit -c web/core/phpunit.xml.dist --testdox web/modules/custom/do_streams/tests/src/Kernel/UserActivityViewTest.php'` → all 6 tests pass (`Tests: 6, Assertions: 135`, only pre-existing `#[RunTestsInSeparateProcesses]` deprecations, 0 failures/errors). Full `do_streams` Kernel directory regression: `Tests: 31, Assertions: 936`, 0 failures/errors (24 pre-existing deprecations, matches F's baseline). `do_chrome` HelpText unit suite: 16/16, zero regressions.
- **Lint:** `DoStreamsHooks.php` — 0 errors, 4 pre-existing warnings (matches F's report exactly). Repaired test file — 11 errors/1 warning, all pre-existing (CRLF line endings, docblock style) and located OUTSIDE my 2-line diff hunk; confirmed via `git stash`/`git diff` that these predate my edit.
- **Tier 2 — AA contrast:** confirmed via grep that `profile-activity.css` introduces zero new colors; `--gc-color-text-muted` appears only in a comment explaining reuse, never redeclared. PASS.
- **Tier 2 — HelpText append-only:** confirmed via `git diff` the `HelpText.php` change is a pure append after the existing `showcase_help.*` block; zero existing keys touched. PASS.
- **Found (NEW blocker, not a test-authorship issue — routed to F, did NOT patch F's code):** Spun up a fully assembled + seeded ddev site (site:install → config:import → seed step_700 chain → cache:rebuild) and ran `tests/e2e/profile-activity.spec.ts` against it: **2/2 FAIL.** Test 1 (the core deliverable — "Recent posts" heading + Maria's own seeded topics, viewed as Maria herself) fails because the block **never renders at all**, for any viewer, confirmed via a faithful in-process HTTP-kernel reproduction (real login + real route matching, confirming `/user/2`'s route DOES correctly resolve `user` → User entity #2). Root cause, traced to Drupal core source (`ArgumentPluginBase::hasDefaultArgument()` / `defaultActions()`): `views.view.user_activity.yml`'s `uid` argument sets `default_action: 'not found'` (a hard-fail/hide-view action), but `default_argument_type: user` (F's correctly-derived default-argument plugin) is ONLY ever consulted by `ViewExecutable::_buildArguments()` when `default_action` is `'default'` — with `'not found'`, `hasDefaultArgument()` always returns FALSE regardless of `default_argument_type`, so the view hard-fails to "hide view" for every viewer, including the profile owner viewing their own page via the block (which supplies no literal arg, relying entirely on the default-argument mechanism). Verified directly: the argument-default plugin's own `getArgument()` correctly resolves `'2'` from the route — it is simply never called. **The fix is a one-line YAML value change** (`default_action: 'not found'` → `'default'`) in `docs/groups/config/views.view.user_activity.yml`, the only wrong piece in an otherwise correctly-derived argument config.
- **Found (non-blocking, flagged for O/A, not fixed):** E2E test 2 (anonymous partial-render) gets a 403 visiting `/user/{uid}` at all — `config/sync/user.role.anonymous.yml` does not grant `'access user profiles'` to anonymous on this repo's baseline permission set, and no other spec in the repo visits `/user/{uid}` unauthenticated. Whether anonymous SHOULD reach profile pages is a product question outside T's authority to decide unilaterally; left for O/A.
- **Evidence:** `web/core/modules/views/src/ViewExecutable.php` (`_buildArguments()`, lines 1119-1204); `web/core/modules/views/src/Plugin/views/argument/ArgumentPluginBase.php` (`hasDefaultArgument()`/`defaultActions()`/`getDefaultArgument()`, lines 597-645, 873-896); `web/core/modules/views/src/Plugin/Block/ViewsBlock.php` (`build()`, confirms block-context args, not a literal arg, are what's passed); `web/core/modules/user/src/Plugin/views/argument_default/User.php` (confirmed `getArgument()` resolves correctly given a real route match); `config/sync/user.role.anonymous.yml`; direct in-process HTTP-kernel + drush `php:script` reproductions (throwaway, not committed).
- **Ready for:** F must apply the one-line `default_action` fix and re-verify; O/A must decide the anonymous-profile-access question before test 2 can be judged pass/fail on its merits. T-GREEN is NOT clear to hand off to U — a real, live-browser-confirmed functional gap exists in the core deliverable.

## F — Phase 5 rev-1 (ST-5 one-line YAML fix)

- **Decided:** Applied T-GREEN's diagnosed one-line fix in `docs/groups/config/views.view.user_activity.yml`: changed the `uid` contextual argument's `default_action` from `'not found'` to `default`. Confirmed independently via direct core source read (not just trusting T's note) — `ArgumentPluginBase::defaultActions()` (web/core/modules/views/src/Plugin/views/argument/ArgumentPluginBase.php:597-631) defines `'default'` with `'has default argument' => TRUE` and `'not found'` with `'hard fail' => TRUE`; `hasDefaultArgument()` (line 883-886) and `adminSummary()` (line 1457) both gate strictly on the literal string `'default'`. This confirms `default_argument_type: user` (and its `default_argument_options: { user: false }`) were only ever dead configuration under the old value — the fix makes Views actually consult that plugin. No other field touched; `git diff` shows exactly one changed line.
- **Verified:** Re-ran `bash scripts/ci/assemble-config.sh` via `ddev exec` (clean, 130 config files copied) and re-ran `UserActivityViewTest.php`: all 6 tests still pass (`Tests: 6, Assertions: 135`, 0 failures/errors — identical assertion count to T-GREEN's prior baseline), plus a full `do_streams` Kernel-directory regression check (`Tests: 31, Assertions: 936`, 0 failures/errors, matching the pre-existing baseline exactly — zero regressions). E2E re-verification against the live seeded site is explicitly T's re-verify pass per this task's scope, not run here.
- **Evidence:** `web/core/modules/views/src/Plugin/views/argument/ArgumentPluginBase.php` (`defaultActions()` lines 597-631, `hasDefaultArgument()` lines 883-886, `adminSummary()` line 1457); ddev Kernel test runs as above.

## T — Phase 6 rev-1 (E2E re-verify after F's one-line YAML fix + O's scoping decision)

- **Decided:** Adjusted the E2E outsider-viewer case in `tests/e2e/profile-activity.spec.ts` per O's scoping decision (2026-07-23): the brief's "outsider viewer doesn't see content from groups they can't access" AC is about a logged-in visitor who lacks group membership, not an anonymous visitor (baseline anonymous `access user profiles` permission is a separate site-level concern, out of scope for #114). Re-authored to authenticate as `ravi_patel` (seeded member of "DrupalCon Portland 2026" only per step_700_demo_data.php lines 98/99/101 — confirmed NOT a member of "Core Committers" or "Leadership Council", Maria's two private groups) instead of an anonymous context.
- **Found (new, genuine site-level gap, NOT fixed):** Running the re-authored outsider case exposed that `authenticated` (not just `anonymous`) also lacks `access user profiles` — confirmed via `drush role:list --format=json`. Drupal core's `UserAccessControlHandler::checkAccess()` requires this permission (or `administer users`) to view any OTHER user's `/user/{uid}`; every non-admin authenticated persona 403s on someone else's profile. Confirmed no existing spec in the repo covers this case: `phase4.spec.ts`'s only precedent visits `/user/1` as `admin` — i.e., an admin viewing THEIR OWN profile (uid 1), which short-circuits via `administer users` before `access user profiles` is ever consulted.
- **Decided:** Per the task's offered fallback, **deleted the outsider-access E2E case** as redundant coverage rather than leave it blocked on an unrelated, out-of-scope permissions gap. The exact behavior (access-scoped partial render for a non-member viewer) is already fully and correctly pinned by the Kernel tests `testAccessScopingExcludesPrivateGroupNodeForNonMember` + `testAccessScopingIncludesPrivateGroupNodeForMember`, which exercise the view directly with an explicit uid argument and do not depend on the separate, currently-broken `/user/{uid}` route-permission question at all.
- **Found (new, test-authorship bug in my own original RED test, NOT F's or O's issue — fixed here):** The surviving "renders on Maria's profile" E2E case also failed on re-run, on its DOM-order (newest-first) assertion. Root-caused via direct SQL query against the seeded site: all three of Maria's seeded nodes (nid 1, 6, 10) share the IDENTICAL `created` timestamp (1784847624) because `step_700_demo_data.php` never sets an explicit `created` value and its sequential `$node->save()` calls complete within the same wall-clock second. A tied `created DESC` sort legitimately falls back to natural/nid order (ascending) — the live page correctly renders oldest-first for this seed data. This is not a view bug (`created DESC` is present and correct, confirmed by reading `views.view.user_activity.yml`) and not a regression from F's fix — it is my original E2E assertion's premise (strictly-increasing seed-time timestamps) being invalid against the actual seed script.
- **Decided:** Deleted the invalid DOM-order assertion (kept heading + all-three-titles-present assertions, which remain valid and are the core deliverable this E2E proves). Ordering is already fully and rigorously pinned at the correct, cheaper tier by the Kernel test `testResultsOrderNewestFirst`, which sets three explicit, strictly-increasing `created` values ($base/$base+100/$base+200) specifically to make the DESC sort observable — patching the seed script or re-authoring a flaky same-second workaround was judged disproportionate.
- **Re-verification — Kernel (sanity, no regression):** `ddev exec` assemble (clean, 130 files) → `phpunit UserActivityViewTest.php` → 6/6 pass, `Tests: 6, Assertions: 135` — identical to the prior T-GREEN baseline. Zero regressions from F's rev-1 fix.
- **Re-verification — E2E (GREEN):** `ddev exec 'drush config:import -y'` (picked up F's rev-1 YAML fix + block config) → `cache:rebuild` → `BASE_URL="https://gm114-profile.ddev.site" npx playwright test tests/e2e/profile-activity.spec.ts` → **1 passed** (ran twice for stability after a transient first-run cache staleness from the demo persona-switch mechanism colliding with the just-completed config:import; both re-runs clean).
- **Net effect on suite:** `tests/e2e/profile-activity.spec.ts` now contains one test (down from two), each removal justified by either (a) duplicating Kernel coverage while blocked on an out-of-scope site permission gap, or (b) an invalid premise about seed-data timestamp granularity — not by weakening coverage of any acceptance criterion. Suite is proportionate: every AC still has a passing test at the correct tier.
- **Verdict:** T-green complete, no blocking issues. Kernel 6/6 GREEN, E2E 1/1 GREEN (structurally exits 0). Ready for U (UI surface).
- **Evidence:** `docs/groups/scripts/step_700_demo_data.php` (lines 98/99/101 group membership; lines 140/145/149 topic seeding order; no explicit `created` set); `web/core/modules/user/src/UserAccessControlHandler.php` (`checkAccess()`, lines 30-54); `config/sync`/`drush role:list --format=json` (`authenticated` role perms, no `access user profiles`); `tests/e2e/phase4.spec.ts` (only prior `/user/{uid}` precedent, admin-viewing-self); direct SQL query against `node_field_data` for nids 1/6/10; `docs/groups/config/views.view.user_activity.yml` (`sorts.created`, `order: DESC`, `granularity: second`); Kernel + E2E run transcripts as above (full detail in `docs/handoffs/st5-profile-114/handoff-T-green.md` "T — Phase 6 rev-1" section).

## O — post-T-green
- **Decided:** Accept T's removal of the outsider-viewer E2E case as redundant with the Kernel access-scoping tests (private-group + non-member scenarios covered with real Group entities). Accept removal of the DOM-order E2E assertion since seed nodes share timestamps; Kernel test 4 pins ordering rigorously with distinct t1/t2/t3.
- **Assumed:** The `access user profiles` gap on `anonymous` + `authenticated` roles is a site-baseline concern outside #114's scope. Will surface in the Chain Summary as a follow-up candidate (not filing a GH issue per POC-no-follow-ups rule; will simply flag when the operator sees the final summary).
- **Evidence:** T-GREEN rev-1 handoff; `config/sync/user.role.anonymous.yml` inspection by T.

## U — Phase 8 (UI walkthrough)
- **Verdict:** PASS. Live UI on `https://gm114-profile.ddev.site` matches wireframe across all in-scope states.
- **Verified — Maria own profile (many state):** `<h2>Recent posts</h2>` renders (class `block__title`); all 3 seeded topics present as anchor links with correct hrefs (`/node/1` Sprint Planning: Portland 2026, `/node/6` Weekly Standup Notes, `/node/10` Budget Allocation Q3 2026). Each row is a `stream_card` render with author (maria_chen), group badge (DrupalCon Portland 2026 / Core Committers / Leadership Council), type badge (Forum), relative-date meta, snippet, and comment count. Title link click navigates to `/node/1` correctly.
- **Verified — Empty state (fresh user `u_walk_fresh` uid 8):** `<h2>Recent posts</h2>` present; body renders exactly "No posts yet." inside `.view-empty` — matches wireframe copy verbatim.
- **Verified — Mobile 360px:** After a `drush cr` the block renders identically on Maria's profile at 360×800 (heading + 3 stream cards stacked, no horizontal overflow). First combined-context run had a stale internal page-cache miss producing an empty block; a cache rebuild resolved it and mobile has rendered cleanly on every subsequent load. Not a story-level bug — session/cache noise from repeated persona-switches in one browser context, not from the block itself. Fresh browser contexts render correctly.
- **Verified — WCAG 2.2 AA spot-checks:**
  - Heading is real `<h2 class="block__title">Recent posts</h2>`, not a styled div; siblings the existing profile blocks' `<h2>`s under the page `<h1>maria_chen</h1>`. No skipped levels.
  - Tab-to-link focus indicator visible: `outline: rgb(27, 39, 51) solid 2px` on the first row's `<a>` — high-contrast dark outline on white card background, clearly meets AA non-text 3:1.
  - Muted "Type · date" text uses theme colors, not a new shade: computed `color: rgb(43, 53, 59)` on the empty-state text (dark on white ≈ 12.6:1, way above 4.5:1). F committed to `--gc-color-text-muted` reuse; grep of `profile-activity.css` confirms zero new color declarations.
  - No color-only status anywhere in the block; type is text badge, date is text.
- **Access-safety spot-check (OUT OF SCOPE, noted):** Ravi visiting Maria's `/user/2` returns 403 — confirms the site-baseline `access user profiles` gap already flagged by T/O (authenticated role lacks that perm). Kernel access-scoping tests cover the underlying view logic; not U's to resolve.
- **Console:** One `ReferenceError: jQuery is not defined` pageerror surfaces on `/user/{uid}` — grepped `docs/groups/modules/do_streams` for `jQuery`, zero matches, so this is not introduced by this story. Pre-existing platform-level issue outside #114 scope. No 500s. The 403s in the log are exactly Ravi's expected access-safety probe.
- **Evidence:** screenshots at `C:/Users/aange/AppData/Local/Temp/claude/C--Users-aange-Projects-groups-on-d11/3f8c6656-8990-47c8-9917-3ecdcd64c1ce/scratchpad/u114/` (`maria-own-desktop.png`, `mobile-alone.png`, `empty-fresh-user.png`, `maria-own-focus.png`, `click-nav.png`); walkthrough handoff at `docs/handoffs/st5-profile-114/handoff-U.md`.

## S — Phase 9 (spec audit)
- **Verdict:** PASS. Every issue AC + every brief checkbox pinned to at least one green test at the correct tier (see handoff-S.md AC coverage table for 11-row mapping). Scope-disjoint diff (3 new files + 3 append/extend on the "Owns" list), reuse-disciplined (mirrors `group_content_stream` shape + `following_feed` block-precedent + existing `preprocessViewsView` guard convention), no parallel path invented.
- **Verified — access-safety:** Kernel `testAccessScopingExcludesPrivateGroupNodeForNonMember` + sanity companion `...IncludesPrivateGroupNodeForMember` use REAL Group entities via `GroupsKernelTestBase` (`createGroup`/`addMember`/`addNode`) and toggle viewer via `setCurrentUser` — not simulated. Sanity companion rules out vacuous exclusion. `testPublishedOnlyExcludesUnpublishedNode` closes the third AC.
- **Verified — WCAG 2.2 AA:** U-agent evidence (handoff-U checks 1/4/7 + spot-check block) confirms real `<h2>`, 2px focus outline `rgb(27, 39, 51)`, muted-text contrast ≈ 12.6:1 (well above 4.5:1), no color-only status, access-safe "No posts yet." empty-state copy.
- **Verified — HelpText:** append-only diff (`profile_activity.section`), zero existing keys touched, distinct-namespace rationale vs. reserved `page.profile_stream` (#126) documented in-line.
- **Verified — test quality:** 6 Kernel + 1 E2E is proportionate to a 5-guarantee feature story; T rev-1's decision to demote ordering from E2E → Kernel-only was correct (E2E premise of strictly-increasing seed timestamps was invalidated by same-second saves). No mock-shaped/tautological/coverage-padding tests.
- **Advisories (non-blocking, not follow-ups per POC memory rule — flagged for O to notice at merge):** (1) site-baseline `access user profiles` permission gap on both `anonymous` and `authenticated` roles will affect every future profile-surface story; (2) pre-existing `jQuery is not defined` console error on `/user/{uid}` (not from `do_streams`); (3) `.ddev/config.yaml` project-name collisions across worktrees (housekeeping).
- **Evidence:** handoff-S.md; direct inspection of `docs/groups/config/views.view.user_activity.yml` L14/L30-42/L43-63/L79-129/L140-144/L177-183; `block.block.do_streams_user_activity.yml` L11-29; `profile-activity.css` (2 scoped rules, 0 colors); `UserActivityViewTest.php` (all 6 tests read); F/T/U handoff transcripts.
