# Handoff-A: Phase 3 - #113 ST-4 Trending surface (/trending)  (up-front plan review)

**Date:** 2026-07-23
**Branch:** 113-trending
**Brief reviewed:** `docs/planning/handoffs/113-trending/brief.md`
**Reuse map:** `docs/planning/handoffs/113-trending/survey.md`
**Wireframe:** `docs/planning/handoffs/113-trending/wireframe.md`
**Verdict:** PASS

## Summary
The plan is a clean, minimal extension of established patterns. Zero new modules/services/plugins; one new views config cloned from `following_feed.yml` with the sort block copied from `hot_content.yml`; one CSS file mirroring `following.css`; one library entry mirroring the `following:` block; append-only marker blocks in `deploy/entrypoint.sh` and `.github/workflows/test.yml`. All seven author-visible questions resolve in favor of the brief as written, with one small clarification on the library-attach mechanism (see Finding 3 — advisory, no amendment required). T may proceed to author the RED suite.

## Findings

| # | Severity | Plan element | Drift dimension | Finding | Suggested fix |
|---|---|---|---|---|---|
| 1 | PASS | Empty-copy divergence (view says `"Nothing trending yet."`; shell scope entry says `"Nothing is trending right now. Check back soon."`) | copy consistency | Brief's rationale is correct. `/trending` is a plain Views page, NOT a `do_streams_shell` consumer; the two strings live on two unconnected render paths. Verified: `DoStreamsHooks::preprocessDoStreamsShell()` at line 499 sets `empty_copy` only for shell-consumer render arrays keyed on `#theme => do_streams_shell`; the trending view emits its own `empty.area_text_custom` and never enters that theme hook. Issue body copy wins for this route. Reunification is out of scope. | None. Keep issue-body copy verbatim in the view. |
| 2 | PASS | No ranking pill on `/trending` (single fixed sort) | pattern consistency | Matches the analogous plain-views-page pattern exactly. `hot_content.yml` and `following_feed.yml` both render as plain Views pages with no ranking pill — the pill is a shell-only construct (`ranking_control` var built in `preprocessDoStreamsShell`). This story explicitly does not attach to the shell (non-scope §), so omitting the pill is correct. Rendering a one-option, permanently-active pill would be inert interactive chrome (WCAG concern). | None. |
| 3 | PASS (with advisory) | Library attach mechanism | pattern consistency | The idiomatic pattern is **already established in this same file** and should be reused verbatim rather than added as a new `views_pre_render` hook. `DoStreamsHooks::preprocessViewsView()` at lines 399-406 already guards on `$view->id() === self::FOLLOWING_FEED_VIEW_ID` and appends `do_streams/following` to `$variables['#attached']['library']`. The trending attach should be a **second `id()` check inside the same method** (or a follow-up early-return branch), not a new hook. Brief permits F to choose the wiring — this advisory narrows it: **extend the existing `preprocessViewsView` method with a `TRENDING_VIEW_ID` constant + second guard**, mirroring the do_showcase pattern via the do_streams pattern. This keeps the class's "one lightweight preprocess method that guards on view id" convention intact (see the class docblock at line 41 referencing this exact convention). | Advisory to F: add `const TRENDING_VIEW_ID = 'trending';` and a second guard branch in `preprocessViewsView()`. No brief amendment required — this is within the brief's "F may prefer... whichever is more idiomatic" latitude. |
| 4 | PASS | Duplication risk — cloning `hot_content.yml` vs. adding a `page_2` display to it | duplication / dependency direction | The clone is correct. (a) Issue AC explicitly forbids editing `hot_content.yml` (regression guard). (b) `hot_content.yml` exposes admin-facing fields (`score`, `type`, `title`, `created` numeric-labeled) and uses `view_mode: teaser` at 25/page — a materially different presentation surface from the story's `stream_card` at 10/page. A shared multi-display config would either compromise `hot_content`'s admin surface or bloat the config with per-display overrides that duplicate the current file anyway. (c) The Reuse map's chosen approach — clone `following_feed.yml` shape + copy `hot_content.yml`'s sort block — is the correct extend-the-analogous-object move (following_feed is the analogous stream_card page; hot_content is the analogous sort source). Neither is being duplicated in full. | None. |
| 5 | PASS (with advisory) | `access.type: none` (public) — group-node access interference | cross-cutting concerns / access | Group relationships exist for `community_group-group_node-{post,page,event,forum,doc}`, so group-owned nodes ARE gated by `gnode`'s node-access records. However this is the SAME model that `/hot` already runs under successfully (public route, same base table, same content types, in production for weeks). The `community_group-anon_view` config-provisioned role grants anon `view group_node:*` on published groups (see `deploy/entrypoint.sh` lines 122-135 comment). Ungrouped nodes remain publicly viewable via core `access content`. So on the seeded demo, "Venue Logistics Thread" and "Patch Review Process RFC" will be visible to anon iff their owning groups are published (they are, per step_700 seed). | Advisory to T: the e2e spec's "top-2 titles appear" assertion should tolerate the (unlikely) case where node-access filters those threads out — a helpful assertion is "spec fails LOUDLY on 200-with-empty" rather than a silent falsy match, so anon-vs-node-access mismatch surfaces as a real failure rather than a mystery. Brief acceptance list already implies this. No amendment. |
| 6 | PASS | Sibling collision with #116 on `deploy/entrypoint.sh` + `.github/workflows/test.yml` | integration | Confirmed. #116's markers are `# --- do_activity step_7xx BEGIN/END ---` (verified: `entrypoint.sh` lines 155/177, `test.yml` lines 558/575). Story's markers are `# --- do_discovery cron BEGIN/END ---`. Distinct namespaces → append-only blocks merge cleanly regardless of order. Any conflict would be a trivial adjacent-hunk hunk marker, not a semantic overlap. | None. |
| 7 | PASS | Cron placement in workflow — AFTER do_activity END block, BEFORE `cache:rebuild` | ordering / cross-cutting | Confirmed correct. Verified `test.yml` line 575 is the `# --- do_activity step_7xx END ---` marker and line 576 is `cache:rebuild`. Placing `drush cron` between these two points ensures: (a) all seeded comments exist before cron (needed to compute `comment_count × 3`); (b) `cache:rebuild` runs after cron so any views cached before the score table populated is invalidated before Playwright hits `/trending`. Same reasoning for `entrypoint.sh`: brief specifies "after `$DRUSH cr`... before closing `fi`" — reviewers should note that in the entrypoint the `cr` line (179) already runs LAST inside the `else` branch; inserting cron AFTER `$DRUSH cr` means the cache is warm-but-stale for `do_discovery_hot_score` until the next request rebuilds. Acceptable — hot_score is not cached-per-request; the view executes a fresh JOIN each render. If F prefers, cron BEFORE `$DRUSH cr` is equally valid and marginally safer. Non-blocking. | Optional: F may swap ordering to `cron` then `cr` in entrypoint. Brief-as-written is acceptable. |

## Notes for O
None (PASS). Brief may proceed unmodified into T(RED). If F wants to codify Finding 3's advisory, the brief's Step 3 last paragraph ("Author F may prefer... whichever is more idiomatic in the existing codebase") already covers it — no edit required.

## Patterns referenced
- `docs/groups/config/views.view.following_feed.yml` — clone base
- `docs/groups/config/views.view.hot_content.yml` — sort-block source; regression guard
- `docs/groups/modules/do_streams/src/Hook/DoStreamsHooks.php:399-406` — existing `preprocess_views_view` library-attach idiom (advisory §3)
- `docs/groups/modules/do_streams/do_streams.libraries.yml` + `css/following.css` — mirror pattern
- `deploy/entrypoint.sh:155-177` and `.github/workflows/test.yml:558-576` — established BEGIN/END marker convention from #116
